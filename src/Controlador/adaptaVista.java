package controlador;

public class adaptaVista 
{
	 void adaptaTren(String miTren) // Recibe el String de la interfaz y lo transforma en datos legibles por el mundo (básicamente para el método creaTren())
	 {
		 // ¿qué formato tiene esa String?
	 }
	 void adaptaSe�al(String miSe�al) // Recibe el String de la interfaz y lo transforma en datos legibles por el mundo (básicamente para el método creaSeñal())
	 {
		 // ¿qué formato tiene esa String?
	 }
	 void adaptaTrayecto(String miTrayecto) // Recibe el String de la interfaz y lo transforma en datos legibles por el mundo (básicamente para el método creaTrayecto())
	 {
		 // ¿qué formato tiene esa String?
	 }
}
